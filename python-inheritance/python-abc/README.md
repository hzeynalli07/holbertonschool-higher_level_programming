# Python - Abstract Base Classes (ABC)

This project explores the use of Abstract Base Classes in Python to define blueprints for other classes.

## Tasks
* **0. Abstract Animal Class and its Subclasses**: Implementation of an abstract class `Animal` and its subclasses `Dog` and `Cat`.
